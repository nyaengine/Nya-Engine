local engine3DUI = {}

--engine ui only libraries
local something = require("lib/something")

--UI objects
local group
local projectName
local disX, disY
local disSizeX, disSizeY
local FontDropdown
local ThemeDropdown

local scrollOffset = 0
local scrollSpeed = 20 -- Speed of scrolling

function engine3DUI.load()

end

function engine3DUI.draw()
    if InEngine then

    end
end

function engine3DUI.update(dt)
    if InEngine then

    end
end

return engine3DUI